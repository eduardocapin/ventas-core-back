import { ProvincesRepository } from './provinces.repository';

describe('ProvincesRepository', () => {
  it('should be defined', () => {
    expect(new ProvincesRepository()).toBeDefined();
  });
});
