---
description: Orquestador único del workspace - AI Manager, agentes, skills y normas en IA_MANAGER_TEMPLATE
alwaysApply: true
---

# Orquestador de Agentes del Workspace

En este workspace, el **orquestador de agentes, skills y normas** es **siempre** el definido en la carpeta (ruta relativa a la raíz del workspace):

**`{{IA_MANAGER_TEMPLATE_PATH}}`**

No uses otro conjunto de reglas o agentes para orquestar. Usa únicamente esta instancia como fuente de contexto.

## Rutas de contexto (relativas a la raíz del workspace)

- **Manager (orquestador):** `{{IA_MANAGER_TEMPLATE_PATH}}/00_CORE_MANAGER/00_MANAGER.md`
- **Registro de agentes:** `{{IA_MANAGER_TEMPLATE_PATH}}/00_CORE_MANAGER/AGENTS_REGISTRY.json`
- **Contexto global (normas, diccionario, guardrails, checkpoints):** `{{IA_MANAGER_TEMPLATE_PATH}}/01_GLOBAL_CONTEXT/`
- **Definiciones de agentes:** `{{IA_MANAGER_TEMPLATE_PATH}}/02_AGENTS_REGISTRY/`
- **Logs de auditoría y deuda técnica:** `{{IA_MANAGER_TEMPLATE_PATH}}/00_CORE_MANAGER/Audit_Logs.md`, `Technical_Debt.md`

## Comportamiento obligatorio

1. **Al inicio de sesión:** Consulta `01_GLOBAL_CONTEXT/LAST_SESSION_STATUS.md` dentro del template para retomar el trabajo.
2. **Antes de cambios profundos:** Lee `01_GLOBAL_CONTEXT/Reglas_Generales.md`, `Diccionario.md`, `AI_Safety_Guardrails.md` del template.
3. **Para delegar tareas:** Usa `AGENTS_REGISTRY.json` y las definiciones en `02_AGENTS_REGISTRY/` del template.
4. **Workspace Isolation:** Opera solo sobre archivos bajo la raíz del workspace; el contexto de orquestación viene del template.

Al crear nuevos proyectos dentro de este workspace, **no** copies otra instancia de IA_MANAGER_TEMPLATE; sigue usando esta carpeta como orquestador único.
